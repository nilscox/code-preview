{
  "name": "@code-preview/root",
  "packageManager": "pnpm@7.27.0",
  "scripts": {
    "build": "pnpm -r build",
    "build:watch": "pnpm -r --parallel build --watch",
    "test": "pnpm -r test"
  }
}
